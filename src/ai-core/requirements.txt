# =============================================================================
# AI Core Dependencies - Production
# =============================================================================

# Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
gunicorn==22.0.0
pydantic==2.9.2
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.25
greenlet>=3.0.3
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.1

# Email
aiosmtplib==3.0.1
email-validator==2.3.0

# Cache
redis==5.0.1
aioredis==2.0.1

# LLM & AI
# openai client is reused for both Groq (OpenAI-compatible chat API, see
# infrastructure/llm/provider_factory.py) and RAG embeddings.
# >=1.55 required: older 1.x releases pass httpx.AsyncClient(proxies=...),
# a kwarg httpx removed in 0.28 - GroqLLMProvider.__init__ crashed with
# "AsyncClient.__init__() got an unexpected keyword argument 'proxies'"
# until bumped. Stays on the 1.x line deliberately (not the 3.x line,
# which depends on an experimental "httpx2" package).
openai==1.109.1

# Vector Store
# numpy pinned <2.0: chromadb 0.4.22 calls np.float_, removed in NumPy 2.0 -
# left unpinned it silently resolves to numpy 2.x, chromadb's client fails to
# init, and the app falls back to an empty in-memory vector store (RAG then
# finds 0 products even when the catalog isn't empty, no error surfaced).
numpy==1.26.4
chromadb==0.4.22

# HTTP
httpx==0.26.0
# >=3.14.3: CVE-2026-69244 (DoS via malformed HTTP responses), fixed upstream.
aiohttp==3.14.3

# Monitoring & Logging
prometheus-client==0.19.0
structlog==24.1.0
python-json-logger==2.0.7

# Security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4

# Utilities
python-dotenv==1.0.1
tenacity==8.2.3
python-multipart==0.0.30

# =============================================================================
# Development Dependencies (requirements-dev.txt)
# =============================================================================

