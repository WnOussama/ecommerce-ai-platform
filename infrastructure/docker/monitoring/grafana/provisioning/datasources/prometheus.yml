apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus # matches the hardcoded datasource UID in ai-observability.json
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
